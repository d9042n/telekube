apiVersion: v1
kind: Service
metadata:
  name: {{ include "telekube.fullname" . }}
  labels:
    {{- include "telekube.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: health
      protocol: TCP
      name: health
  selector:
    {{- include "telekube.selectorLabels" . | nindent 4 }}
